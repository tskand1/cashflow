export interface NavItem {
  label: string;
  href: string;
}

export interface Step {
  number: string;
  title: string;
  description: string;
}

export interface Benefit {
  title: string;
  description: string;
}

export interface AssetType {
  title: string;
  description: string;
  iconName: string;
}